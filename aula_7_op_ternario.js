var sexo = "M"

var res;
        
if (sexo == "F"){
    res = true

} else {
    res = false
};

// bastante util quando tem um if else 
var retorno = (sexo === "M") ? true : false;
console.log(retorno);